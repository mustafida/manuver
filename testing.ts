import { db } from './src/lib/server/db/index.ts'; import { garduInduk, penyulang } from './src/lib/server/db/schema.ts'; import { eq, sql } from 'drizzle-orm'; async function run(){try{const res = await db.select({id: garduInduk.id, nama: garduInduk.nama, count: sql\count(\)\}).from(garduInduk).leftJoin(penyulang, eq(garduInduk.id, penyulang.garduIndukId)).groupBy(garduInduk.id); console.log(res); process.exit(0);}catch(e){console.error('DB ERROR:', e); process.exit(1);}} run();